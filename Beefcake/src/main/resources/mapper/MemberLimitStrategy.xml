<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.MemberLimitMapper">
    <select id="getMax" resultType="int">

        select max_member from member_limit_strategy;
    </select>

    <select id="getMin" resultType="int">
        select min_member from member_limit_strategy;
    </select>

</mapper>