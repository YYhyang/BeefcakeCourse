<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 指定工作空间，要与接口名相同，源代码没有去看，猜测应该是通过"这里的namespace.下边方法的id"来定位方法的 -->
<mapper namespace="com.example.demo.Mapper.KlassStudentMapper">

    <insert id="ask">
        insert into question(student_id, seminar_id, team_id, present_id, round) value (#{arg0},#{arg1},#{arg2},#{arg3},#{arg4})
    </insert>

    <select id="getStudentIdByTeamId" resultType="java.lang.Long">
        select student_id from klass_student where team_id=#{teamId}
    </select>

    <select id="getTeamId" resultType="java.lang.Long">
        select team_id from klass_student where student_id=#{studentId} and course_id=#{courseId}
    </select>

    <update id="addTeamMember">
        update klass_student set team_id=#{teamId} where klass_id=#{klassId} and student_id=#{studentId}
    </update>

    <update id="deleteTeamMember">
        update klass_student set team_id=NULL  where team_id=#{teamId} and student_id=#{studentId}
    </update>

    <select id="getNoTeamStudentsId" resultType="java.lang.Long">
        select student_id from klass_student where course_id = #{courseId} and team_id is null
    </select>

    <select id="getCoursesIdByStudentId" resultType="java.lang.Long">
        select course_id from klass_student where student_id = #{studentId}
    </select>
</mapper>