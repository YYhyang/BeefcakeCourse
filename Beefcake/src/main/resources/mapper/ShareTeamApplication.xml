<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.Mapper.ShareTeamApplicationMapper">
    <select id="getShareTeamApplication" resultType="com.example.demo.Entity.ShareApplicationEntity">
        select * from share_team_application where main_course_id = #{mainCourseId} and sub_course_id =#{subCourseId};
    </select>

    <select id="getShareById" resultType="com.example.demo.Entity.ShareApplicationEntity">
        select * from share_team_application where id = #{Id}
    </select>

    <select id="getShareTeamRequest" resultType="com.example.demo.Entity.ShareApplicationEntity">
        select * from share_team_application where sub_course_teacher_id=#{subTeacherId} and status is null;
    </select>

    <update id="setStatusNull">
        update share_team_application set status = null  where main_course_id = #{mainCourseId} and sub_course_id =#{subCourseId};
    </update>

    <insert id="createShareTeamApplication">
        insert into share_team_application(main_course_id,sub_course_id,sub_course_teacher_id) values (#{mainCourseId},#{subCourseId},#{subCourseTeacherId})
    </insert>

    <select id="getId" resultType="java.lang.String">
        select id from share_team_application where main_course_id = #{mainCourseId} and sub_course_id =#{subCourseId};
    </select>

    <select id="getTeamSharesId" resultType="java.lang.Long">
        select id from share_team_application where main_course_id = #{courseId} or sub_course_id =#{courseId};
    </select>

    <select id="getStatus" resultType="java.lang.String">
        select status from share_team_application where id = #{shareId};
    </select>

    <delete id="deleteTeamShare">
        delete from share_team_application where id = #{Id};
    </delete>

    <update id="setStatus">
        update share_team_application set status=#{status} where id=#{shareId};
    </update>

    <select id="getShareTeamById" resultType="com.example.demo.Entity.ShareApplicationEntity">
        select * from share_team_application where id= #{shareId};
    </select>
</mapper>